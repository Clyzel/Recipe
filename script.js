let backgroundChange =
    document.querySelector('body');
backgroundChange.addEventListener('click', () =>
      backgroundChange.style.backgroundColor = 'yellow');

let buttonChange = document.querySelector('#noblink');
    buttonChange.addEventListener('click', () => buttonChange.style.backgroundColor = '#4cae4c');